
{% func RenderCheckbox(name string) %}

<div class="c-app-checkbox">
    <input class="c-checkbox" type="checkbox" id="{%s name %}" name="{%s name %}">
    <label for="{%s name %}"></label>
</div>

{% endfunc %}